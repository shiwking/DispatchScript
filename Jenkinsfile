pipeline {
    agent any 
    stages {
        stage('Run Case') {
            steps {
                sh 'python3 -u /var/jenkins_home/DispatchScript/DistributeScripts.py'
            }

        }
    }
  }
