pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps { 
                sh 'echo  python3 /var/jenkins_home/DispatchScript/DistributeScripts.py' 
            }
        }
    }
  }
